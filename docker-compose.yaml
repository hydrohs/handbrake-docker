version: '3'

services:
  handbrake: 
    build: .
    ports:
      - "2201:22"
      - "10001:10000"
    volumes:
      - ~/.ssh/authorized_keys:/authorized_keys:ro
      - config:/config
      - /storage:/storage
      - /output:/output
      - /watch:/watch
    env_file:
      - .env
    restart: unless-stopped

volumes:
  config:
